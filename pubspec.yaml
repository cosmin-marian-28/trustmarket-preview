name: buy_sell
description: A new Flutter project.

version: 1.7.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  # Firebase - keeping for Cloud Functions and FCM
  firebase_core: ^3.10.0
  firebase_messaging: ^15.0.0
  cloud_functions: ^5.3.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.0.0
  # Supabase - for database, storage, and auth
  supabase_flutter: ^2.5.0
  file_picker: ^10.3.7
  google_generative_ai: ^0.3.0
  image: ^4.3.0
  flutter_image_compress: ^2.0.0
  shared_preferences: ^2.2.3
  cached_network_image: ^3.2.3
  flutter_cache_manager: ^3.3.0
  flutter_local_notifications: ^18.0.1
  # Identity verification dependencies (aligned to known-good project)
  camera: ^0.10.5+5
  video_thumbnail: ^0.5.0
  google_mlkit_face_detection: ^0.13.1
  google_mlkit_commons: ^0.11.0
  path_provider: ^2.1.0
  permission_handler: ^11.0.1
  flutter_stripe: ^11.5.0
  http: ^1.1.0
  universal_html: ^2.2.0
  flutter_svg: ^2.0.0
  country_flags: ^3.0.0
  webview_flutter: ^4.10.0
  url_launcher: ^6.2.0
  geolocator: ^10.1.0
  geocoding: ^2.1.1
  uuid: ^4.5.1
  sensors_plus: ^6.1.1
  audioplayers: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/card_logos/
    - assets/icons/
    - assets/purchase.mp3